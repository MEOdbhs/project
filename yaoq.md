请按照以下要求执行React到Vue的代码迁移任务：

1. 首先全面分析react文件夹下的项目结构，重点查看YGX-SECMP-dev项目中DataIntelligence、DynamicRisk、MonitoringSurveillance这三个模块（文件夹里的index.vue里有注释是什么模块页面）的对应的完整代码实现，包括：
   - 页面布局结构与CSS样式定义
   - 组件层级关系与props传递
   - 状态管理逻辑（useState、useEffect等）
   - 事件处理函数与业务逻辑
   - API接口调用与数据格式

2. 将转换后的代码更新到YGX-SECMP-dev项目中DataIntelligence、DynamicRisk、MonitoringSurveillance这三个模块（文件夹里的index.vue里有注释是什么模块页面）里，确保：
   - 一比一精准还原原有页面布局和视觉样式
   - 保持原有的交互逻辑和用户体验
   - 迁移所有文本内容、图标、颜色方案

3. 组件转换规范：
   - 表格列表：必须使用YGX-SECMP-dev项目中的BasicTable组件，配置相同的列定义、分页参数、排序规则
   - 弹窗表单：新增、编辑、查看功能必须使用BasicModal+BasicForm组件组合，保持字段验证规则、表单布局、按钮位置一致
   - 图表组件：保持原有图表类型、配色方案、交互效果
   - 其他自定义组件：必须完全还原原有样式和功能，包括动画效果、响应式行为

4. 代码规范要求：
   - 参考YGX-SECMP-dev项目system文件夹下其他模块的代码实现方式
   - 统一使用项目的命名规范、文件结构、代码风格
   - 保持组件导入路径、变量命名、方法命名的一致性
   - 使用项目统一的工具函数和公共方法
   - 自定义组件则以react文件夹中的为主

5. 质量验收标准：
   - 界面样式与React版本像素级一致
   - 所有功能点完整可用，无缺失或异常
   - 响应式布局在不同屏幕尺寸下表现一致
   - 控制台无报错，性能表现良好
   - 通过功能测试、样式对比测试、兼容性测试

6. 交付物要求：
   - 三个模块完整的.vue文件及配套资源
   - 组件使用说明文档（props、events、slots定义）
   - 样式差异对比报告
   - 功能验证测试报告
